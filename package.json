# In your proxy-server directory
echo {
  "name": "proxy-server",
  "version": "1.0.0",
  "description": "HTTPS proxy server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Build completed'"
  },
  "dependencies": {
    "express": "^4.17.1",
    "http-proxy-middleware": "^2.0.1",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=14"
  }
} > package.json